// * 首页地址（默认）
export const HOME_URL = "/home/index";

// * 登录页地址（默认）
export const LOGIN_URL = "/login";

// * Tabs（白名单地址，不添加到 tabs 的路由地址）
export const WHITE_LIST: string[] = ["/403", "/404", "/500", LOGIN_URL];